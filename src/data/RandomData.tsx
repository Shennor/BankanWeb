

/*

import {randomInt} from "crypto";

const randMax = 10000
let users: number[] = []

// CardButton
function randomCardInfo(){
    return new CardInfo(randomInt(randMax),
        "Name ${ randomInt(10000) }",
        randomInt(randMax),
        ,
        deadline,
        randomInt(randMax),
        JSON("{}")
        )
};

// List
function randomListInfo();

// Board
function randomBoardInfo();

// Workspace
function randomWorkspaceInfo();

// Settings
function randomSettingsInfo();

// User
function randomUserInfo();
*/

export{}